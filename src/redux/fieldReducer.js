const initialState = {
    field: [[null, null, null],
            [null, null, null],
            [null, null, null]]
};

export const fieldReducer = (state = initialState, action) => {

    return state;
};